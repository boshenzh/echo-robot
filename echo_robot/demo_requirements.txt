# Demo Script Requirements
# Run: pip install -r demo_requirements.txt

# Core dependencies
opencv-python>=4.8.0
ultralytics>=8.0.0
numpy>=1.21.0

# Optional audio (for advanced demo)
pygame>=2.5.0

# Robot control (if using real hardware)
# lerobot  # Install separately if using real robot hardware

# System requirements note:
# For audio alerts on Linux: sudo apt-get install alsa-utils
# For speaker-test command: sudo apt-get install alsa-utils
